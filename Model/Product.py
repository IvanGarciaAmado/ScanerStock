class Product():
    def __init__(self, name, quantity, codeBar):
        name=self.name
        quantity=self.quantity

    def str(self):
        return {
            "name":self.name,
            "quantity": self.quantity
            }